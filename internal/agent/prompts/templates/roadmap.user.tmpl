{{- /*
  roadmap.user: the Plan/Roadmap-phase user prompt for INITIAL roadmap
  creation.

  When a design/design artifact is present, the design doc is the
  authoritative spec — its contents already cover the feature
  description and visual references, so we only point at the path and
  suppress the redundant Feature / Visual References blocks. In Medium
  mode (no design doc) those blocks ARE the spec, so they render.

  Repo-task routing lives in the later phase plan, where `**Repo:** <name>`
  task tags are the source of truth. The roadmap prompt only signals the
  authoritative feature-wide repo set so agents do not infer scope from
  sibling directories in the worktree parent.
*/ -}}
{{- define "roadmap.user" -}}
# Planning Context

{{ if .RefactorPassForkPoint }}## Refactor Pass Context

This feature is a refactor pass forked from its parent feature's delivered state at {{ .RefactorPassForkPoint }}. The workspace already reflects everything the parent delivered, and that state is what the feature description refers to: resolve any language about prior, current, or existing state against the fork point, not the repository base branch or any earlier state. The description decides the delta — it may change anything the fork point contains. All work is confined to the worktrees listed for this pass: other checkouts of the same repositories exist and are off-limits.

Because reviewers later judge the assembled result as a whole, the roadmap you produce must resolve any such relative language into absolute statements of the intended end state (what each affected artifact must look like when this pass completes), so no downstream reader has to guess which baseline a phrase refers to. If the description is genuinely ambiguous about the intended end state, ask rather than guess.

{{ end }}{{ if .DesignArtifactPath }}Design Document: {{ .DesignArtifactPath }}

{{ end }}{{ if .ResearchArtifactPath }}Research Document: {{ .ResearchArtifactPath }}

{{ end }}{{ if not .DesignArtifactPath }}## Feature

**Name**: {{ .Name }}

**Description**:
> {{ blockquote .Description }}

{{ if .ExitCriteria }}**Exit criteria** (distill these into the roadmap's `## Overall Exit Criteria` section as concrete, observable statements):

> {{ blockquote .ExitCriteria }}

{{ end }}{{ template "visual_references" .VisualReferences }}{{ end }}{{ template "qa_files" .QAFiles }}{{ if .Repos }}## Target Repositories

This feature targets the repositories below. Treat this list as authoritative; ignore sibling directories that are not listed here.
{{ range .Repos }}- **{{ .Name }}** ({{ .Path }})
{{ end }}
{{ end }}{{ template "grillme_inquireness" .Inquireness }}
{{ end -}}
