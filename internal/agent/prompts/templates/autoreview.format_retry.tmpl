{{- define "autoreview.format_retry" -}}
The previous review did not return a valid decision token. Review the same command under the policy above and reply with exactly ALLOW or DEFER, uppercase, with no explanation, punctuation, Markdown, or other text. Either decision is valid; if uncertain about potentially dangerous effects, reply DEFER.
{{ end -}}
