# Planning Context

## Feature

**Name**: OAuth login

**Description**:
> Sign in with Google.

**Exit criteria** (distill these into the roadmap's `## Overall Exit Criteria` section as concrete, observable statements):

> Google sign-in works end to end and PKCE is enforced.

## Target Repositories

This feature targets the repositories below. Treat this list as authoritative; ignore sibling directories that are not listed here.
- **web** (/repos/web)

## Ambiguity Resolution [grill-me]
Interview me relentlessly about every aspect of this plan until we reach a shared understanding. Walk down each branch of the design tree, resolving dependencies between decisions one-by-one. For each question, provide your recommended answer.

Ask the questions one at a time, waiting for feedback on each question before continuing.

If a question can be answered by exploring the codebase, explore the codebase instead.
