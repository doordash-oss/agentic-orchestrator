{{- /*
  final_review.user: the Final-Review user prompt for the interactive
  reviewer session (full tool access — can read the diff, run tests/build,
  explore the codebase). Distinct from review.user.tmpl which is for the
  per-iteration print-mode review gate that audits the verification report.
*/ -}}
{{- define "final_review.user" -}}
{{ template "visual_references" .VisualReferences }}# Product Acceptance Context

**Review iteration**: {{ .Iteration }}
**Diff base**: {{ .DiffBase }}
{{ if .FeedbackPath }}**Review feedback output**: {{ .FeedbackPath }}
{{ end }}

{{ if or .DesignArtifactPath .FeatureDescription }}## Approved User Intent

{{ if .DesignArtifactPath }}**Approved design**: {{ .DesignArtifactPath }}

{{ end }}
{{ if .FeatureDescription }}
{{ .FeatureDescription }}

{{ end }}{{ end }}{{ if .ExitCriteria }}## Exit Criteria

{{ .ExitCriteria }}

{{ end }}{{ if .RoadmapPath }}## Optional Scope Context

**Roadmap**: {{ .RoadmapPath }}

{{ end }}{{ if .CycleFocus }}## Current Cycle Focus

{{ .CycleFocus }}

{{ end }}{{ if or .PriorImplementationReportPaths .PriorImplementationEvidenceRootDirs .PriorImplementationEvidenceArtifacts }}## Reusable Last-Phase QA Context

The following evidence can help avoid duplicating QA work when it still proves the current behavior under review. Use it as optional context, not as a checklist.

{{ if .PriorImplementationReportPaths }}Latest completed implementation verification reports:
{{ range .PriorImplementationReportPaths }}- {{ . }}
{{ end }}{{ end }}{{ if .PriorImplementationEvidenceRootDirs }}Evidence artifact roots:
{{ range .PriorImplementationEvidenceRootDirs }}- {{ . }}
{{ end }}{{ end }}{{ if .PriorImplementationEvidenceArtifacts }}Referenced evidence artifacts:
{{ range .PriorImplementationEvidenceArtifacts }}- {{ . }}
{{ end }}{{ end }}
{{ end }}{{ if not .Publishable }}NOTE: Local-only repository — no remote (origin) available.
{{ end }}
{{- end -}}
