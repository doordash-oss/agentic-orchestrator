{{- /*
  refactor_plan.user: the Refactor-plan step invocation arguments. The
  RoleSpec system prompt owns primary skill discovery, Useful Resources,
  output roots, and completion. The refactor SKILL.md owns output format,
  per-repo tag rules, and the planning-only boundary.
*/ -}}
{{- define "refactor_plan.user" -}}
# Refactor Plan

## Refactor Request

{{ .Request }}

{{ if .FeatureContext }}## Feature Context

{{ .FeatureContext }}

{{ end }}## Workspace

Repos in this feature:
{{ range .Repos -}}
- `{{ .Name }}` - `{{ .Path }}`
{{ end -}}
{{ end -}}
