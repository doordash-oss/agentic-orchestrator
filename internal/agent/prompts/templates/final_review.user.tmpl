{{- /*
  final_review.user: the Final-Review user prompt for the interactive
  reviewer session (full tool access — can read the diff, run tests/build,
  explore the codebase). Distinct from review.user.tmpl which is for the
  per-iteration print-mode review gate that audits the verification report.
*/ -}}
{{- define "final_review.user" -}}
{{ template "visual_references" .VisualReferences }}# Review Context

**Review iteration**: {{ .Iteration }}
**Diff base**: {{ .DiffBase }}

## Feature Intent

{{ if .RoadmapPath }}**Roadmap**: {{ .RoadmapPath }}
{{ end }}
{{ if .ExitCriteria }}**Exit criteria**:
{{ .ExitCriteria }}

{{ end }}{{ if .CycleFocus }}## Current Cycle Focus

{{ .CycleFocus }}

{{ end }}{{ if .TestingContractPath }}## Verification Context

**Testing contract**: {{ .TestingContractPath }}
**Verification report**: {{ .VerificationPath }}

{{ else if .VerificationPath }}**Verification report**: {{ .VerificationPath }}
{{ end }}{{ if or .PriorImplementationPlanPaths .PriorImplementationContractPaths .PriorImplementationReportPaths .PriorImplementationEvidenceRootDirs .PriorImplementationEvidenceArtifacts }}## Prior Implementation Evidence

{{ if .PriorImplementationPlanPaths }}Phase plans:
{{ range .PriorImplementationPlanPaths }}- {{ . }}
{{ end }}{{ end }}{{ if .PriorImplementationContractPaths }}Implementation testing contracts:
{{ range .PriorImplementationContractPaths }}- {{ . }}
{{ end }}{{ end }}{{ if .PriorImplementationReportPaths }}Latest completed implementation verification reports:
{{ range .PriorImplementationReportPaths }}- {{ . }}
{{ end }}{{ end }}{{ if .PriorImplementationEvidenceRootDirs }}Evidence artifact roots:
{{ range .PriorImplementationEvidenceRootDirs }}- {{ . }}
{{ end }}{{ end }}{{ if .PriorImplementationEvidenceArtifacts }}Referenced evidence artifacts:
{{ range .PriorImplementationEvidenceArtifacts }}- {{ . }}
{{ end }}{{ end }}
{{ end }}{{ if not .Publishable }}NOTE: Local-only repository — no remote (origin) available.
{{ end }}
{{- end -}}
