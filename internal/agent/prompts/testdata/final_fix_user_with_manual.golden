## Visual References

IMPORTANT: The user attached the image(s) below when creating this feature. They communicate intent that text alone cannot — mockups, desired-state screenshots, annotated whiteboards, bug repros, or reference designs. Before applying this fix, Read each image via tool-use and treat its visual information as a binding specification for what the output should look like. If an image shows a specific layout, palette, typography, or interaction, match it; do not substitute generic defaults.

- [Image #1]: /tmp/login.png

# Fix Context

This is fix iteration 2.

**Acceptance**:
Feature acceptance is defined by the `## Acceptance Criteria` section of the approved design at /state/feat-x/run-1/design/design.md. Judge acceptance against that section, not against any restatement of it.

## Reviewer Feedback

Read the reviewer feedback at: /state/feat-x/run-1/review/iteration-2/review-feedback.md

## Manual Verification Outcomes

The reviewer flagged unattested `### Manual Verification` bullets. Re-running automated commands does NOT attest a manual bullet. For each flagged bullet, describe in your fix output what you actually observed when you executed it: steps, command output snippets, UI state. "Manual check, looks fine" is not evidence.

If the bullet is genuinely out-of-environment (production-only, physical device, mutates real user state), do not invent an observation — state the gap and its downstream owner in your fix output so the reviewer can judge it.

