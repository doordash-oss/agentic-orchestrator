{{- /*
  phase_plan_revision.user: the per-phase plan revision user prompt.
  Always strict-inquireness=none (revisions don't re-open decisions).

  Feature name and prior phase plan paths are omitted. Approved visual
  references remain in scope because critic-driven task revisions must not
  drift from the Design contract.
*/ -}}
{{- define "phase_plan_revision.user" -}}
{{ template "visual_references" .VisualReferences }}{{ if .PhasePlanFormatPath }}The methodology references the plan output format at: {{ .PhasePlanFormatPath }} — read this file in addition to the SKILL.md before producing the revised plan.

{{ end }}# Revision Context

Revision attempt {{ .Attempt }} for Phase {{ .Phase.Number }} — {{ .Phase.Name }}

## Critic Feedback

{{ .Feedback }}

{{ template "prior_axis_approvals" .PriorAxisApprovals }}{{ if .PhasePlanPath }}## Plan to Revise

Plan: {{ .PhasePlanPath }}

{{ end }}{{ if .AutomatedVerificationOnly }}## Verification Contracting Mode

This feature verifies through automated tests only.

- Put every verification command — unit, integration, build, lint, and packaged end-to-end journeys — under `### Automated Verification`.
- `### Manual Verification`, `### Visual Evidence`, and `### Behavioral Evidence` must each contain exactly one item: `- [ ] None required: automated-only verification for this feature`.
- Do not contract screenshots, manual observations, or any other agent-captured evidence artifacts.

{{ end }}{{ template "autonomous_inquireness" .Inquireness }}
{{ end -}}
