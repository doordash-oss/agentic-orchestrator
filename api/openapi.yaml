openapi: 3.1.0
info:
  title: Agentico Server API
  version: v1
  description: Loopback REST and SSE API for the Agentico headless runtime.
servers:
  - url: http://127.0.0.1:{port}
    variables:
      port:
        default: "0"
security:
  - bearerAuth: []
paths:
  /api/v1/health:
    get:
      operationId: getHealth
      summary: Read runtime health and launch metadata.
      description: >-
        Liveness-only endpoint (status, runtime identity, launch policy,
        start time, owner — no secrets). Intentionally exempt from bearer
        auth so that a stale discovery token can't make a live, healthy
        server look dead to PrepareDiscovery.
      security: []
      responses:
        "200":
          $ref: "#/components/responses/HealthResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/features:
    get:
      operationId: listFeatures
      summary: List feature dashboard summaries.
      responses:
        "200":
          $ref: "#/components/responses/FeatureListResponse"
        "304":
          $ref: "#/components/responses/NotModified"
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      operationId: createFeature
      summary: Create a feature.
      parameters:
        - $ref: "#/components/parameters/TrustedMutationHeader"
      requestBody:
        $ref: "#/components/requestBodies/JSONMutation"
      responses:
        "201":
          $ref: "#/components/responses/ActionResponse"
        "400":
          $ref: "#/components/responses/ErrorResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/features/{feature_id}:
    get:
      operationId: getFeature
      summary: Read one feature.
      parameters:
        - $ref: "#/components/parameters/FeatureID"
      responses:
        "200":
          $ref: "#/components/responses/FeatureDetailResponse"
        "304":
          $ref: "#/components/responses/NotModified"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/ErrorResponse"

  /api/v1/features/{feature_id}/config:
    get:
      operationId: getFeatureConfig
      summary: Read feature configuration.
      parameters:
        - $ref: "#/components/parameters/FeatureID"
      responses:
        "200":
          $ref: "#/components/responses/FeatureConfigResponse"
        "304":
          $ref: "#/components/responses/NotModified"
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      operationId: updateFeatureConfig
      summary: Update feature configuration.
      parameters:
        - $ref: "#/components/parameters/FeatureID"
        - $ref: "#/components/parameters/TrustedMutationHeader"
      requestBody:
        $ref: "#/components/requestBodies/JSONMutation"
      responses:
        "200":
          $ref: "#/components/responses/ActionResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/features/{feature_id}/live-preview:
    get:
      operationId: getFeatureLivePreview
      summary: Read a feature's compact live preview.
      parameters:
        - $ref: "#/components/parameters/FeatureID"
      responses:
        "200":
          $ref: "#/components/responses/LivePreviewResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/features/{feature_id}/reviews:
    post:
      operationId: createReviewSession
      summary: Create or reopen the active artifact review draft session.
      parameters:
        - $ref: "#/components/parameters/FeatureID"
        - $ref: "#/components/parameters/TrustedMutationHeader"
      requestBody:
        $ref: "#/components/requestBodies/JSONMutation"
      responses:
        "200":
          $ref: "#/components/responses/ReviewSessionResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/ErrorResponse"

  /api/v1/features/{feature_id}/reviews/{review_id}/draft:
    put:
      operationId: saveReviewDraft
      summary: Replace the draft text for one artifact review session.
      parameters:
        - $ref: "#/components/parameters/FeatureID"
        - $ref: "#/components/parameters/ReviewID"
        - $ref: "#/components/parameters/TrustedMutationHeader"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReviewDraftUpdateRequest"
      responses:
        "200":
          $ref: "#/components/responses/ReviewSessionResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/ErrorResponse"

  /api/v1/features/{feature_id}/reviews/{review_id}/decision:
    post:
      operationId: submitReviewSessionDecision
      summary: Commit a review draft and submit its review decision.
      parameters:
        - $ref: "#/components/parameters/FeatureID"
        - $ref: "#/components/parameters/ReviewID"
        - $ref: "#/components/parameters/TrustedMutationHeader"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReviewSessionDecisionRequest"
      responses:
        "200":
          $ref: "#/components/responses/ReviewSessionDecisionResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/ErrorResponse"

  /api/v1/features/{feature_id}/actions/{action}:
    post:
      operationId: runFeatureAction
      summary: Run a named feature action.
      parameters:
        - $ref: "#/components/parameters/FeatureID"
        - $ref: "#/components/parameters/FeatureAction"
        - $ref: "#/components/parameters/TrustedMutationHeader"
      requestBody:
        $ref: "#/components/requestBodies/JSONMutation"
      responses:
        "200":
          $ref: "#/components/responses/ActionResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/features/{feature_id}/actions/{action}/{subaction}:
    post:
      operationId: runFeatureSubaction
      summary: Run a named feature subaction.
      parameters:
        - $ref: "#/components/parameters/FeatureID"
        - $ref: "#/components/parameters/FeatureAction"
        - $ref: "#/components/parameters/FeatureSubaction"
        - $ref: "#/components/parameters/TrustedMutationHeader"
      requestBody:
        $ref: "#/components/requestBodies/JSONMutation"
      responses:
        "200":
          $ref: "#/components/responses/ActionResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/features/{feature_id}/runs/{run_number}/artifacts:
    get:
      operationId: listArtifacts
      summary: List artifacts for one feature run.
      parameters:
        - $ref: "#/components/parameters/FeatureID"
        - $ref: "#/components/parameters/RunNumber"
      responses:
        "200":
          $ref: "#/components/responses/ArtifactListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/features/{feature_id}/runs/{run_number}/artifacts/{artifact_id}:
    get:
      operationId: getArtifactContent
      summary: Read a bounded text artifact slice.
      parameters:
        - $ref: "#/components/parameters/FeatureID"
        - $ref: "#/components/parameters/RunNumber"
        - $ref: "#/components/parameters/ArtifactID"
        - $ref: "#/components/parameters/Offset"
        - $ref: "#/components/parameters/Limit"
      responses:
        "200":
          $ref: "#/components/responses/TextContentResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/features/{feature_id}/runs/{run_number}/logs/{log_id}:
    get:
      operationId: getRunLogContent
      summary: Read a bounded run log slice.
      parameters:
        - $ref: "#/components/parameters/FeatureID"
        - $ref: "#/components/parameters/RunNumber"
        - $ref: "#/components/parameters/LogID"
        - $ref: "#/components/parameters/Offset"
        - $ref: "#/components/parameters/Limit"
      responses:
        "200":
          $ref: "#/components/responses/TextContentResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/config/runtime:
    get:
      operationId: getRuntimeConfig
      summary: Read runtime configuration.
      responses:
        "200":
          $ref: "#/components/responses/RuntimeConfigResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
    patch:
      operationId: patchRuntimeConfig
      summary: Patch runtime configuration.
      parameters:
        - $ref: "#/components/parameters/TrustedMutationHeader"
      requestBody:
        $ref: "#/components/requestBodies/JSONMutation"
      responses:
        "200":
          $ref: "#/components/responses/ActionResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
    put:
      operationId: putRuntimeConfig
      summary: Replace runtime configuration.
      parameters:
        - $ref: "#/components/parameters/TrustedMutationHeader"
      requestBody:
        $ref: "#/components/requestBodies/JSONMutation"
      responses:
        "200":
          $ref: "#/components/responses/ActionResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/catalog/models:
    get:
      operationId: listModels
      summary: List available provider models.
      responses:
        "200":
          $ref: "#/components/responses/ModelCatalogResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/prompts:
    get:
      operationId: listPrompts
      summary: List pending prompt requests.
      responses:
        "200":
          $ref: "#/components/responses/PromptSnapshotResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/prompts/ask-user/answer:
    post:
      operationId: answerAskUserPrompt
      summary: Answer an ask-user prompt.
      parameters:
        - $ref: "#/components/parameters/TrustedMutationHeader"
      requestBody:
        $ref: "#/components/requestBodies/JSONMutation"
      responses:
        "200":
          $ref: "#/components/responses/ActionResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/prompts/help/send:
    post:
      operationId: sendHelpPrompt
      summary: Send a help message to a session.
      parameters:
        - $ref: "#/components/parameters/TrustedMutationHeader"
      requestBody:
        $ref: "#/components/requestBodies/JSONMutation"
      responses:
        "200":
          $ref: "#/components/responses/ActionResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/prompts/chat/start:
    post:
      operationId: startChatPrompt
      summary: Start a chat prompt session.
      parameters:
        - $ref: "#/components/parameters/TrustedMutationHeader"
      requestBody:
        $ref: "#/components/requestBodies/JSONMutation"
      responses:
        "200":
          $ref: "#/components/responses/ActionResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/permissions:
    get:
      operationId: listPermissions
      summary: List pending permission requests.
      responses:
        "200":
          $ref: "#/components/responses/PermissionSnapshotResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/permissions/answer:
    post:
      operationId: answerPermission
      summary: Answer a permission request.
      parameters:
        - $ref: "#/components/parameters/TrustedMutationHeader"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PermissionAnswerRequest"
      responses:
        "200":
          $ref: "#/components/responses/ActionResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/sessions:
    get:
      operationId: listSessions
      summary: List active and recent sessions.
      responses:
        "200":
          $ref: "#/components/responses/SessionListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/sessions/{session_id}:
    get:
      operationId: getSession
      summary: Read a session summary.
      parameters:
        - $ref: "#/components/parameters/SessionID"
      responses:
        "200":
          $ref: "#/components/responses/SessionDetailResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/sessions/{session_id}/transcript:
    get:
      operationId: getSessionTranscript
      summary: Read a bounded structured session transcript.
      parameters:
        - $ref: "#/components/parameters/SessionID"
        - name: offset
          in: query
          required: false
          schema:
            type: integer
            format: int64
            minimum: 0
        - $ref: "#/components/parameters/Limit"
      responses:
        "200":
          $ref: "#/components/responses/TranscriptResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/sessions/{session_id}/output/stream:
    get:
      operationId: streamSessionOutput
      summary: Stream indexed transcript records as SSE.
      description: >-
        Emits `session.output` events carrying structured `SessionOutputChunk`
        records — the same transcript rows and index space `/transcript`
        returns — and a terminal `session.output.done` event.
      security:
        - bearerAuth: []
        - sseAccessToken: []
      parameters:
        - $ref: "#/components/parameters/SessionID"
        - name: from
          in: query
          required: false
          description: >-
            Resume position expressed as a transcript row index (the same
            index space `/transcript` uses), not a byte offset.
          schema:
            type: integer
            format: int64
            minimum: 0
      responses:
        "200":
          $ref: "#/components/responses/SessionOutputStream"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/recovery:
    get:
      operationId: getRecoverySnapshot
      summary: Scan recoverable sessions.
      responses:
        "200":
          $ref: "#/components/responses/RecoverySnapshotResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/recovery/actions:
    post:
      operationId: executeRecoveryActions
      summary: Execute recovery actions from a recovery snapshot.
      parameters:
        - $ref: "#/components/parameters/TrustedMutationHeader"
      requestBody:
        $ref: "#/components/requestBodies/JSONMutation"
      responses:
        "200":
          $ref: "#/components/responses/ActionResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/shutdown:
    post:
      operationId: shutdownRuntime
      summary: Request runtime shutdown.
      parameters:
        - $ref: "#/components/parameters/TrustedMutationHeader"
      requestBody:
        $ref: "#/components/requestBodies/JSONMutation"
      responses:
        "200":
          $ref: "#/components/responses/ActionResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/events:
    get:
      operationId: streamEvents
      summary: Stream ordered runtime events as SSE.
      description: Emits envelope JSON under SSE `data:` lines. Every non-control event includes a monotonic `seq`, `epoch`, resource identity, and resource version. Clients resume with `Last-Event-ID` or `after`.
      security:
        - bearerAuth: []
        - sseAccessToken: []
      parameters:
        - name: after
          in: query
          required: false
          schema:
            type: integer
            format: int64
            x-go-type: uint64
            minimum: 0
        - name: epoch
          in: query
          required: false
          schema:
            type: string
        - name: heartbeat_ms
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
      responses:
        "200":
          $ref: "#/components/responses/EventStream"
        "401":
          $ref: "#/components/responses/Unauthorized"

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: instance-token
      description: Per-instance token from discovery metadata.
    sseAccessToken:
      type: apiKey
      in: query
      name: access_token
      description: SSE fallback for browser EventSource clients that cannot set Authorization.

  parameters:
    FeatureID:
      name: feature_id
      in: path
      required: true
      schema:
        type: string
    RunNumber:
      name: run_number
      in: path
      required: true
      schema:
        type: integer
        minimum: 1
    ArtifactID:
      name: artifact_id
      in: path
      required: true
      schema:
        type: string
    ReviewID:
      name: review_id
      in: path
      required: true
      schema:
        type: string
    LogID:
      name: log_id
      in: path
      required: true
      schema:
        type: string
    SessionID:
      name: session_id
      in: path
      required: true
      schema:
        type: string
    FeatureAction:
      name: action
      in: path
      required: true
      schema:
        type: string
        enum:
          - start
          - resume
          - pause-stop
          - restart
          - publish
          - merge
          - rewind
          - retry
          - rebase
          - review-comments
          - review-decision
          - refactor
          - need-user-input
          - need-user-input-draft
          - mark-done
          - cleanup
          - delete
    FeatureSubaction:
      name: subaction
      in: path
      required: true
      schema:
        type: string
        enum:
          - description
          - fetch
          - finish
          - restart
    Offset:
      name: offset
      in: query
      required: false
      schema:
        type: integer
        format: int64
        minimum: 0
    Limit:
      name: limit
      in: query
      required: false
      schema:
        type: integer
        format: int64
        minimum: 1
    TrustedMutationHeader:
      name: X-Agentico-Client
      in: header
      required: true
      schema:
        type: string
        enum: ["local"]
      description: CSRF defense-in-depth for local browser-origin mutations. Bearer auth is still required.

  requestBodies:
    JSONMutation:
      required: true
      content:
        application/json:
          schema:
            type: object
            additionalProperties: true

  responses:
    HealthResponse:
      description: Runtime health.
      headers:
        X-Agentico-Seq:
          $ref: "#/components/headers/Sequence"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/HealthResponse"
    FeatureListResponse:
      description: Feature list.
      headers:
        X-Agentico-Seq:
          $ref: "#/components/headers/Sequence"
        ETag:
          $ref: "#/components/headers/ETag"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/FeatureListResponse"
    FeatureDetailResponse:
      description: Feature detail.
      headers:
        X-Agentico-Seq:
          $ref: "#/components/headers/Sequence"
        ETag:
          $ref: "#/components/headers/ETag"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/FeatureDetailResponse"
    FeatureConfigResponse:
      description: Feature config.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/FeatureConfigResponse"
    RuntimeConfigResponse:
      description: Runtime config.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/RuntimeConfigResponse"
    ModelCatalogResponse:
      description: Model catalog.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ModelCatalogResponse"
    PromptSnapshotResponse:
      description: Pending prompt snapshot.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/PromptSnapshotResponse"
    PermissionSnapshotResponse:
      description: Pending permission snapshot.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/PermissionSnapshotResponse"
    ArtifactListResponse:
      description: Artifact list.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ArtifactListResponse"
    TextContentResponse:
      description: Bounded text content.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/TextContentResponse"
    ReviewSessionResponse:
      description: Artifact review draft session.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ReviewSessionResponse"
    ReviewSessionDecisionResponse:
      description: Artifact review draft decision result.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ReviewSessionDecisionResponse"
    LivePreviewResponse:
      description: Live preview.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/LivePreviewResponse"
    SessionListResponse:
      description: Session list.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/SessionListResponse"
    SessionDetailResponse:
      description: Session detail.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/SessionDetailResponse"
    TranscriptResponse:
      description: Structured transcript.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/TranscriptResponse"
    RecoverySnapshotResponse:
      description: Recovery snapshot.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/RecoverySnapshotResponse"
    ActionResponse:
      description: Mutation result.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ActionResponse"
    ErrorResponse:
      description: Structured API error.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    Unauthorized:
      description: Missing or invalid bearer token.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    NotModified:
      description: Snapshot revision matches the supplied validator.
    EventStream:
      description: Runtime SSE stream carrying `SSEEvent` JSON envelopes.
      content:
        text/event-stream:
          schema:
            $ref: "#/components/schemas/SSEEvent"
    SessionOutputStream:
      description: Session output SSE stream carrying `SessionOutputChunk` JSON payloads.
      content:
        text/event-stream:
          schema:
            $ref: "#/components/schemas/SessionOutputChunk"

  headers:
    Sequence:
      description: Current event broker sequence at response time.
      schema:
        type: integer
        minimum: 0
    ETag:
      description: Snapshot revision validator.
      schema:
        type: string

  schemas:
    ResponseMeta:
      type: object
      required: [revision, generated_at, as_of_seq]
      properties:
        revision:
          type: string
        generated_at:
          type: string
          format: date-time
        as_of_seq:
          type: integer
          format: int64
          x-go-type: uint64
          minimum: 0
    ErrorResponse:
      type: object
      required: [api_version, error]
      properties:
        api_version:
          type: string
        error:
          $ref: "#/components/schemas/Error"
    Error:
      type: object
      required: [code, message, status]
      properties:
        code:
          type: string
        message:
          type: string
        status:
          type: integer
        target:
          type: object
          additionalProperties: true
    PermissionAnswerRequest:
      x-go-name: PermissionAnswerSchema
      type: object
      required: [request_id, decision]
      properties:
        request_id:
          type: string
        session_id:
          type: string
        decision:
          type: string
          enum: [allow_once, allow_remember, deny]
        remember_pattern:
          type: string
        remember_scope:
          type: string
          description: Existing permission cache scope. Empty string means global.
          x-go-type-skip-optional-pointer: false
    Resource:
      type: object
      required: [type]
      properties:
        type:
          type: string
        id:
          type: string
        feature_id:
          type: string
        phase:
          type: string
    SSEEvent:
      type: object
      required: [api_version, id, kind, at, resource, snapshot_required]
      properties:
        api_version:
          type: string
        id:
          type: string
        seq:
          type: integer
          format: int64
          x-go-type: uint64
          minimum: 0
        epoch:
          type: string
        kind:
          type: string
        at:
          type: string
          format: date-time
        resource:
          $ref: "#/components/schemas/Resource"
        resource_version:
          type: integer
          format: int64
          x-go-type: uint64
          minimum: 0
        revision:
          type: string
        snapshot_required:
          type: boolean
        summary:
          type: string
        record_count:
          type: integer
          description: >-
            Session's current transcript record count at emission time — the
            same index space /transcript and /output/stream use. Only set on
            session.output.activity events.
    SessionOutputChunk:
      type: object
      required: [api_version, index]
      properties:
        api_version:
          type: string
        session_id:
          type: string
        index:
          type: integer
          description: >-
            Transcript row index — the same index space /transcript and
            session.output.activity's record_count use. Not a byte offset.
        message:
          $ref: "#/components/schemas/TranscriptMessage"
          description: A TranscriptMessageDTO row (see the /transcript response).
        done:
          type: boolean
    JSONResponse:
      type: object
      required: [api_version]
      properties:
        api_version:
          type: string
        meta:
          $ref: "#/components/schemas/ResponseMeta"
    HealthResponse:
      allOf:
        - $ref: "#/components/schemas/JSONResponse"
        - type: object
          required: [status, runtime, launch_policy, started_at, owner, server_time]
          properties:
            status:
              type: string
            runtime:
              $ref: "#/components/schemas/RuntimeIdentity"
            launch_policy:
              $ref: "#/components/schemas/LaunchPolicy"
            started_at:
              type: string
              format: date-time
            owner:
              $ref: "#/components/schemas/Owner"
            server_time:
              type: string
              format: date-time
    FeatureListResponse:
      allOf:
        - $ref: "#/components/schemas/JSONResponse"
        - type: object
          required: [features]
          properties:
            features:
              type: array
              items:
                $ref: "#/components/schemas/FeatureSummary"
            warnings:
              type: array
              items:
                $ref: "#/components/schemas/Warning"
    FeatureDetailResponse:
      allOf:
        - $ref: "#/components/schemas/JSONResponse"
        - type: object
          required: [feature]
          properties:
            feature:
              $ref: "#/components/schemas/FeatureDetail"
    FeatureConfigResponse:
      allOf:
        - $ref: "#/components/schemas/JSONResponse"
        - type: object
          required: [feature_id, current, defaults, original, publishability]
          properties:
            feature_id:
              type: string
            current:
              $ref: "#/components/schemas/FeatureConfig"
            defaults:
              $ref: "#/components/schemas/FeatureConfig"
            original:
              $ref: "#/components/schemas/FeatureConfig"
            publishability:
              $ref: "#/components/schemas/Publishability"
              x-go-name: Publish
    RuntimeConfigResponse:
      allOf:
        - $ref: "#/components/schemas/JSONResponse"
        - type: object
          required:
            - runtime
            - model_defaults
            - feature_defaults
            - repos
            - ui
            - notifications
            - observability
            - providers
          properties:
            runtime:
              $ref: "#/components/schemas/RuntimeIdentity"
            model_defaults:
              $ref: "#/components/schemas/ModelDefaults"
              x-go-name: Defaults
            feature_defaults:
              $ref: "#/components/schemas/FeatureDefaults"
            repos:
              type: array
              items:
                $ref: "#/components/schemas/ConfigRepo"
            workspace_roots:
              type: array
              items:
                type: string
            ui:
              x-go-type: config.UIConfig
              x-go-type-import:
                path: github.com/doordash-oss/agentic-orchestrator/internal/config
                name: config
            notifications:
              $ref: "#/components/schemas/NotificationConfig"
            observability:
              $ref: "#/components/schemas/Observability"
            providers:
              type: array
              items:
                type: string
    ModelCatalogResponse:
      allOf:
        - $ref: "#/components/schemas/JSONResponse"
        - type: object
          required: [provider_order, provider_models, phase_defaults, phase_provider_models]
          properties:
            provider_order:
              type: array
              items:
                type: string
            provider_models:
              type: object
              additionalProperties:
                type: array
                items:
                  $ref: "#/components/schemas/Model"
            phase_defaults:
              $ref: "#/components/schemas/ModelDefaults"
            phase_provider_models:
              type: object
              additionalProperties:
                type: object
                additionalProperties:
                  type: array
                  items:
                    type: string
    PromptSnapshotResponse:
      allOf:
        - $ref: "#/components/schemas/JSONResponse"
        - type: object
          required: [ask_user_questions, help_queue, need_user_inputs]
          properties:
            ask_user_questions:
              type: array
              items:
                $ref: "#/components/schemas/ControlRequest"
            help_queue:
              type: array
              items:
                $ref: "#/components/schemas/HelpQueue"
            need_user_inputs:
              type: array
              items:
                $ref: "#/components/schemas/NeedUserInputGate"
    PermissionSnapshotResponse:
      allOf:
        - $ref: "#/components/schemas/JSONResponse"
        - type: object
          required: [requests]
          properties:
            requests:
              type: array
              items:
                $ref: "#/components/schemas/ControlRequest"
    ArtifactListResponse:
      allOf:
        - $ref: "#/components/schemas/JSONResponse"
        - type: object
          required: [artifacts]
          properties:
            artifacts:
              type: array
              items:
                $ref: "#/components/schemas/Artifact"
    TextContentResponse:
      allOf:
        - $ref: "#/components/schemas/JSONResponse"
        - type: object
          required: [id, offset, limit, size, text, truncated]
          properties:
            id:
              type: string
            offset:
              type: integer
              format: int64
            limit:
              type: integer
              format: int64
            size:
              type: integer
              format: int64
            text:
              type: string
            truncated:
              type: boolean
    ReviewSessionResponse:
      allOf:
        - $ref: "#/components/schemas/JSONResponse"
        - type: object
          required:
            - feature_id
            - review_id
            - review_mode
            - target_phase
            - run_number
            - artifact_id
            - text
            - draft_revision
            - source_revision
            - can_iterate
          properties:
            feature_id:
              type: string
            review_id:
              type: string
            review_mode:
              type: string
            target_phase:
              type: string
            run_number:
              type: integer
            artifact_id:
              type: string
            text:
              type: string
            draft_revision:
              type: string
            source_revision:
              type: string
            can_iterate:
              type: boolean
    ReviewDraftUpdateRequest:
      type: object
      required: [base_revision, text]
      properties:
        base_revision:
          type: string
        text:
          type: string
    ReviewSessionDecisionRequest:
      type: object
      required: [decision, base_revision]
      properties:
        decision:
          type: string
        base_revision:
          type: string
    ReviewSessionDecisionResponse:
      allOf:
        - $ref: "#/components/schemas/ActionBaseResponse"
        - type: object
          required: [feature_id, review_id, result]
          properties:
            feature_id:
              type: string
            review_id:
              type: string
            decision:
              type: string
            result:
              type: string
    LivePreviewResponse:
      allOf:
        - $ref: "#/components/schemas/JSONResponse"
        - type: object
          required: [feature, activity, context, timing, cost, transcript]
          properties:
            feature:
              $ref: "#/components/schemas/FeatureSummary"
            session:
              $ref: "#/components/schemas/SessionSummary"
              x-go-type-skip-optional-pointer: false
            activity:
              type: string
            attention:
              type: array
              items:
                $ref: "#/components/schemas/ControlRequest"
            context:
              $ref: "#/components/schemas/Context"
            timing:
              $ref: "#/components/schemas/Timing"
            cost:
              $ref: "#/components/schemas/Cost"
            transcript:
              type: array
              items:
                $ref: "#/components/schemas/TranscriptMessage"
    SessionListResponse:
      allOf:
        - $ref: "#/components/schemas/JSONResponse"
        - type: object
          required: [sessions]
          properties:
            sessions:
              type: array
              items:
                $ref: "#/components/schemas/SessionSummary"
    SessionDetailResponse:
      allOf:
        - $ref: "#/components/schemas/JSONResponse"
        - type: object
          required: [session]
          properties:
            session:
              $ref: "#/components/schemas/SessionDetail"
    TranscriptResponse:
      allOf:
        - $ref: "#/components/schemas/JSONResponse"
        - type: object
          required: [cursor, messages]
          properties:
            cursor:
              $ref: "#/components/schemas/Cursor"
            messages:
              type: array
              items:
                $ref: "#/components/schemas/TranscriptMessage"
    RecoverySnapshotResponse:
      allOf:
        - $ref: "#/components/schemas/JSONResponse"
        - type: object
          required: [snapshot_id, items]
          properties:
            snapshot_id:
              type: string
            items:
              type: array
              items:
                $ref: "#/components/schemas/RecoveryItem"
    ActionBaseResponse:
      $ref: "#/components/schemas/JSONResponse"
    ActionResponse:
      allOf:
        - $ref: "#/components/schemas/ActionBaseResponse"
        - type: object
          properties:
            create_feature_response:
              $ref: "#/components/schemas/CreateFeatureResponse"
            feature_start_response:
              $ref: "#/components/schemas/FeatureStartResponse"
            feature_stop_response:
              $ref: "#/components/schemas/FeatureStopResponse"
            feature_restart_response:
              $ref: "#/components/schemas/FeatureRestartResponse"
            review_decision_response:
              $ref: "#/components/schemas/ReviewDecisionResponse"
            feature_config_update_response:
              $ref: "#/components/schemas/FeatureConfigUpdateResponse"
            need_user_input_decision_response:
              $ref: "#/components/schemas/NeedUserInputDecisionResponse"
            need_user_input_draft_response:
              $ref: "#/components/schemas/NeedUserInputDraftResponse"
            permission_answer_response:
              $ref: "#/components/schemas/PermissionAnswerResponse"
            ask_user_answer_response:
              $ref: "#/components/schemas/AskUserAnswerResponse"
            help_send_response:
              $ref: "#/components/schemas/HelpSendResponse"
            chat_start_response:
              $ref: "#/components/schemas/ChatStartResponse"
            runtime_config_update_response:
              $ref: "#/components/schemas/RuntimeConfigUpdateResponse"
            publish_feature_response:
              $ref: "#/components/schemas/PublishFeatureResponse"
            publish_description_response:
              $ref: "#/components/schemas/PublishDescriptionResponse"
            merge_feature_response:
              $ref: "#/components/schemas/MergeFeatureResponse"
            rewind_feature_response:
              $ref: "#/components/schemas/RewindFeatureResponse"
            retry_feature_response:
              $ref: "#/components/schemas/RetryFeatureResponse"
            rebase_start_response:
              $ref: "#/components/schemas/RebaseStartResponse"
            review_comments_fetch_response:
              $ref: "#/components/schemas/ReviewCommentsFetchResponse"
            review_comments_start_response:
              $ref: "#/components/schemas/ReviewCommentsStartResponse"
            refactor_start_response:
              $ref: "#/components/schemas/RefactorStartResponse"
            refactor_restart_response:
              $ref: "#/components/schemas/RefactorRestartResponse"
            mark_done_response:
              $ref: "#/components/schemas/MarkDoneResponse"
            cleanup_feature_response:
              $ref: "#/components/schemas/CleanupFeatureResponse"
            delete_feature_response:
              $ref: "#/components/schemas/DeleteFeatureResponse"
            recovery_action_response:
              $ref: "#/components/schemas/RecoveryActionResponse"
            shutdown_response:
              $ref: "#/components/schemas/ShutdownResponse"
    CreateFeatureResponse:
      allOf:
        - $ref: "#/components/schemas/ActionBaseResponse"
        - $ref: "#/components/schemas/FeatureActionResult"
    FeatureStartResponse:
      allOf:
        - $ref: "#/components/schemas/ActionBaseResponse"
        - $ref: "#/components/schemas/FeatureActionResult"
        - type: object
          properties:
            phase:
              type: string
            session_ids:
              type: array
              x-go-name: SessionIDs
              items:
                type: string
    FeatureStopResponse:
      allOf:
        - $ref: "#/components/schemas/ActionBaseResponse"
        - $ref: "#/components/schemas/FeatureActionResult"
    FeatureRestartResponse:
      allOf:
        - $ref: "#/components/schemas/ActionBaseResponse"
        - $ref: "#/components/schemas/FeatureActionResult"
        - type: object
          properties:
            phase:
              type: string
            dispatch:
              type: string
            repo_cycle_count:
              type: integer
            refactor_count:
              type: integer
            session_ids:
              type: array
              x-go-name: SessionIDs
              items:
                type: string
    ReviewDecisionResponse:
      allOf:
        - $ref: "#/components/schemas/ActionBaseResponse"
        - $ref: "#/components/schemas/FeatureActionResult"
        - type: object
          required: [decision]
          properties:
            decision:
              type: string
    FeatureConfigUpdateResponse:
      allOf:
        - $ref: "#/components/schemas/ActionBaseResponse"
        - $ref: "#/components/schemas/FeatureActionResult"
    NeedUserInputDecisionResponse:
      allOf:
        - $ref: "#/components/schemas/ActionBaseResponse"
        - $ref: "#/components/schemas/FeatureActionResult"
        - type: object
          required: [decision]
          properties:
            decision:
              type: string
    NeedUserInputDraftResponse:
      allOf:
        - $ref: "#/components/schemas/ActionBaseResponse"
        - $ref: "#/components/schemas/FeatureActionResult"
    PermissionAnswerResponse:
      allOf:
        - $ref: "#/components/schemas/ActionBaseResponse"
        - type: object
          required: [session_id, request_id, decision, result]
          properties:
            session_id:
              type: string
            request_id:
              type: string
            decision:
              type: string
            result:
              type: string
            already_existed:
              type: boolean
            audit_warning:
              type: string
    AskUserAnswerResponse:
      allOf:
        - $ref: "#/components/schemas/ActionBaseResponse"
        - type: object
          required: [session_id, request_id, result]
          properties:
            session_id:
              type: string
            request_id:
              type: string
            result:
              type: string
    HelpSendResponse:
      allOf:
        - $ref: "#/components/schemas/ActionBaseResponse"
        - type: object
          required: [feature_id, session_id, result]
          properties:
            feature_id:
              type: string
            session_id:
              type: string
            result:
              type: string
    ChatStartResponse:
      allOf:
        - $ref: "#/components/schemas/ActionBaseResponse"
        - type: object
          required: [session_id, result]
          properties:
            session_id:
              type: string
            result:
              type: string
    RuntimeConfigUpdateResponse:
      allOf:
        - $ref: "#/components/schemas/ActionBaseResponse"
        - $ref: "#/components/schemas/ActionResult"
    PublishFeatureResponse:
      allOf:
        - $ref: "#/components/schemas/ActionBaseResponse"
        - $ref: "#/components/schemas/FeatureActionResult"
    PublishDescriptionResponse:
      allOf:
        - $ref: "#/components/schemas/ActionBaseResponse"
        - $ref: "#/components/schemas/FeatureActionResult"
        - type: object
          required: [title, body]
          properties:
            title:
              type: string
            body:
              type: string
    MergeFeatureResponse:
      allOf:
        - $ref: "#/components/schemas/ActionBaseResponse"
        - $ref: "#/components/schemas/FeatureActionResult"
    RewindFeatureResponse:
      allOf:
        - $ref: "#/components/schemas/ActionBaseResponse"
        - $ref: "#/components/schemas/FeatureActionResult"
        - type: object
          properties:
            target_phase:
              type: string
            effective_phase:
              type: string
            roadmap_phase:
              type: integer
            warning_count:
              type: integer
            upgrade_pipeline:
              type: string
    RetryFeatureResponse:
      allOf:
        - $ref: "#/components/schemas/ActionBaseResponse"
        - $ref: "#/components/schemas/FeatureActionResult"
    RebaseStartResponse:
      allOf:
        - $ref: "#/components/schemas/ActionBaseResponse"
        - $ref: "#/components/schemas/FeatureActionResult"
        - type: object
          required: [cycle_type]
          properties:
            cycle_type:
              type: string
            session_id:
              type: string
    ReviewComment:
      type: object
      required: [id]
      properties:
        id:
          type: integer
        type:
          type: string
        repo_name:
          type: string
        path:
          type: string
        line:
          type: integer
        body:
          type: string
        user_login:
          type: string
        created_at:
          type: string
        diff_hunk:
          type: string
        in_reply_to_id:
          type: integer
          x-go-name: InReplyTo
    ReviewCommentsFetchResponse:
      allOf:
        - $ref: "#/components/schemas/ActionBaseResponse"
        - type: object
          required: [feature_id, repo, comments]
          properties:
            feature_id:
              type: string
            repo:
              type: string
            mode:
              type: string
            comments:
              type: array
              items:
                $ref: "#/components/schemas/ReviewComment"
    ReviewCommentsStartResponse:
      allOf:
        - $ref: "#/components/schemas/ActionBaseResponse"
        - $ref: "#/components/schemas/FeatureActionResult"
        - type: object
          required: [repo, mode, cycle_type]
          properties:
            repo:
              type: string
            mode:
              type: string
            cycle_type:
              type: string
            comment_count:
              type: integer
            session_id:
              type: string
            source:
              type: string
    RefactorStartResponse:
      allOf:
        - $ref: "#/components/schemas/ActionBaseResponse"
        - $ref: "#/components/schemas/FeatureActionResult"
        - type: object
          required: [cycle_type]
          properties:
            repo:
              type: string
            cycle_type:
              type: string
            pipeline:
              type: string
            session_id:
              type: string
    RefactorRestartResponse:
      allOf:
        - $ref: "#/components/schemas/RefactorStartResponse"
    MarkDoneResponse:
      allOf:
        - $ref: "#/components/schemas/ActionBaseResponse"
        - $ref: "#/components/schemas/FeatureActionResult"
    CleanupFeatureResponse:
      allOf:
        - $ref: "#/components/schemas/ActionBaseResponse"
        - $ref: "#/components/schemas/FeatureActionResult"
        - type: object
          properties:
            target:
              type: string
    DeleteFeatureResponse:
      allOf:
        - $ref: "#/components/schemas/ActionBaseResponse"
        - $ref: "#/components/schemas/FeatureActionResult"
    RecoveryActionResponse:
      allOf:
        - $ref: "#/components/schemas/ActionBaseResponse"
        - $ref: "#/components/schemas/ActionResult"
    ShutdownResponse:
      allOf:
        - $ref: "#/components/schemas/ActionBaseResponse"
        - $ref: "#/components/schemas/ActionResult"
    ActionResult:
      type: object
      required: [result]
      properties:
        result:
          type: string
    FeatureActionResult:
      allOf:
        - $ref: "#/components/schemas/ActionResult"
        - type: object
          required: [feature_id]
          properties:
            feature_id:
              type: string

    RuntimeIdentity:
      type: object
      required: [runtime_dir, state_dir, config_path]
      properties:
        runtime_dir:
          type: string
        state_dir:
          type: string
        config_path:
          type: string
          x-go-name: Config
    LaunchPolicy:
      type: object
      required: [resolved, providers, dangerously_skip_permissions]
      properties:
        resolved:
          type: boolean
        providers:
          type: array
          items:
            type: string
        dangerously_skip_permissions:
          type: boolean
    Owner:
      type: object
      required: [pid, started_at]
      properties:
        pid:
          type: integer
          x-go-name: PID
        pgid:
          type: integer
          x-go-name: PGID
        started_at:
          type: string
          format: date-time
        version:
          type: string
    ConfigRepo:
      type: object
      required: [name]
      properties:
        name:
          type: string
        path:
          type: string
        pipeline_gates:
          type: object
          additionalProperties:
            x-go-type: config.Checkpoints
            x-go-type-import:
              path: github.com/doordash-oss/agentic-orchestrator/internal/config
              name: config
    FeatureDefaults:
      type: object
      required: [models, checkpoints]
      properties:
        models:
          $ref: "#/components/schemas/ModelDefaults"
        effort:
          $ref: "#/components/schemas/EffortConfig"
        pipeline_preferences:
          type: object
          additionalProperties:
            x-go-type: config.PipelinePreference
            x-go-type-import:
              path: github.com/doordash-oss/agentic-orchestrator/internal/config
              name: config
        inquireness:
          type: string
        pipeline:
          type: string
        checkpoints:
          x-go-type: config.Checkpoints
          x-go-type-import:
            path: github.com/doordash-oss/agentic-orchestrator/internal/config
            name: config
        automatic_review_enabled:
          type: boolean
    NotificationConfig:
      type: object
      required: [mute_feature_input]
      properties:
        mute_feature_input:
          type: boolean
    Observability:
      type: object
      required: [events, otel_enabled]
      properties:
        events:
          type: boolean
        otel_enabled:
          type: boolean
          x-go-name: OTelEnabled
        otel_service_name:
          type: string
          x-go-name: OTelServiceName
    FeatureConfig:
      type: object
      required: [models, inquireness, checkpoints]
      properties:
        models:
          $ref: "#/components/schemas/ModelDefaults"
        effort:
          $ref: "#/components/schemas/EffortConfig"
        inquireness:
          type: string
        checkpoints:
          $ref: "#/components/schemas/Checkpoints"
        pipeline:
          type: string
        input_notifications:
          type: string
          enum: [default, enabled, muted]
        automatic_review_mode:
          type: string
          enum: [default, enabled, disabled]
    AutomaticReviewState:
      type: object
      required: [mode, enabled, source]
      properties:
        mode:
          type: string
          enum: [default, enabled, disabled]
        enabled:
          type: boolean
        source:
          type: string
          enum: [global, feature]
    EffortConfig:
      x-go-type: config.EffortConfig
      x-go-type-import:
        path: github.com/doordash-oss/agentic-orchestrator/internal/config
        name: config
      type: object
      properties:
        inquiry:
          type: string
        research:
          type: string
        planning:
          type: string
        implementation:
          type: string
        review:
          type: string
        utilities:
          type: string
        kb_build:
          type: string
    Publishability:
      type: object
      required: [manual_publish, repos]
      properties:
        manual_publish:
          type: boolean
        repos:
          type: object
          additionalProperties:
            type: boolean
    Model:
      type: object
      required: [id]
      properties:
        id:
          type: string
          x-go-name: ID
        display_name:
          type: string
        context_window:
          type: integer
        aliases:
          type: array
          items:
            type: string
        category:
          type: string
        effort_capabilities:
          type: array
          items:
            type: string
          x-go-name: EffortCapabilities
    HelpQueue:
      type: object
      required: [feature_id, question, pending]
      properties:
        feature_id:
          type: string
        question:
          type: string
        pending:
          type: boolean
        time:
          type: string
          format: date-time
    Artifact:
      type: object
      required: [id, type, category, run_number, content_available]
      properties:
        id:
          type: string
        type:
          type: string
        category:
          type: string
        run_number:
          type: integer
        phase:
          type: string
        iteration:
          type: integer
        path:
          type: string
        size:
          type: integer
          format: int64
        modified_at:
          type: string
          format: date-time
        content_available:
          type: boolean
    Context:
      type: object
      required: [percentage]
      properties:
        percentage:
          type: integer
    RecoveryItem:
      type: object
      required: [key, feature_id, process_alive, default_action, allowed_actions]
      properties:
        key:
          type: string
        feature_id:
          type: string
        feature_name:
          type: string
        repo_name:
          type: string
        phase:
          type: string
        iteration:
          type: integer
        pid:
          type: integer
          x-go-name: PID
        process_alive:
          type: boolean
        default_action:
          type: string
        allowed_actions:
          type: array
          items:
            type: string
    Setup:
      type: object
      required: [status]
      properties:
        status:
          type: string
        attempt:
          type: integer
        started_at:
          type: string
          format: date-time
          x-go-type-skip-optional-pointer: false
        completed_at:
          type: string
          format: date-time
          x-go-type-skip-optional-pointer: false
        latest_log_path:
          type: string
        tasks:
          type: object
          additionalProperties:
            $ref: "#/components/schemas/SetupTask"
        task_order:
          type: array
          items:
            type: string
        last_error:
          type: string
    SetupTask:
      type: object
      required: [key, kind, status]
      properties:
        key:
          type: string
        kind:
          type: string
        label:
          type: string
        repo:
          type: string
        status:
          type: string
        path:
          type: string
        source_path:
          type: string
        branch:
          type: string
        start_point:
          type: string
        use_current_branch:
          type: boolean
        attempt:
          type: integer
        started_at:
          type: string
          format: date-time
          x-go-type-skip-optional-pointer: false
        ended_at:
          type: string
          format: date-time
          x-go-type-skip-optional-pointer: false
        last_error:
          type: string
    ToolCall:
      type: object
      properties:
        summary:
          type: string
        prompt:
          type: string
    Task:
      type: object
      properties:
        id:
          type: string
          x-go-name: ID
        tool_use_id:
          type: string
          x-go-name: ToolUseID
        description:
          type: string
        task_type:
          type: string
        prompt:
          type: string
        last_tool_name:
          type: string
        status:
          type: string
        summary:
          type: string
        output_file:
          type: string
    FileChange:
      type: object
      properties:
        path:
          type: string
        old_path:
          type: string
        operation:
          type: string
        detail:
          type: string
        added_lines:
          type: integer
        removed_lines:
          type: integer
        has_diff_patch:
          type: boolean
    TranscriptMessage:
      type: object
      required: [index, role, type]
      properties:
        index:
          type: integer
        block_index:
          type: integer
        role:
          type: string
        type:
          type: string
        text:
          type: string
        tool:
          type: string
        status:
          type: string
        redacted:
          type: boolean
        locally_appended:
          type: boolean
        auto_picked:
          type: boolean
        auto_pick_question:
          type: string
        auto_pick_confidence:
          type: number
          format: double
        file_change:
          $ref: "#/components/schemas/FileChange"
          x-go-type-skip-optional-pointer: false
        tool_call:
          $ref: "#/components/schemas/ToolCall"
          x-go-type-skip-optional-pointer: false
        task:
          $ref: "#/components/schemas/Task"
          x-go-type-skip-optional-pointer: false

    # --- Feature/session payload types backing FeatureListResponse,
    # FeatureDetailResponse, SessionListResponse, SessionDetailResponse.
    # Mirror internal/server/types.go field-for-field.
    Warning:
      type: object
      required: [code, message]
      properties:
        code:
          type: string
        feature_id:
          type: string
        message:
          type: string
    Cycle:
      type: object
      properties:
        type:
          type: string
        status:
          type: string
        count:
          type: integer
        iteration:
          type: integer
    Checkpoints:
      type: object
      required:
        - inquiry_review
        - research_review
        - design_review
        - roadmap_review
        - phase_plan_review
        - manual_publish
        - draft_publish
      properties:
        inquiry_review:
          type: boolean
        research_review:
          type: boolean
        design_review:
          type: boolean
        roadmap_review:
          type: boolean
        phase_plan_review:
          type: boolean
        manual_publish:
          type: boolean
        draft_publish:
          type: boolean
    FeatureProgress:
      type: object
      properties:
        current_iteration:
          type: integer
        current_roadmap_phase:
          type: integer
        total_roadmap_phases:
          type: integer
        current_phase_status:
          type: string
    FeatureSummary:
      type: object
      required:
        - id
        - name
        - slug
        - status
        - current_phase
        - active_run
        - run_count
        - repos
        - created_at
        - checkpoints
        - progress
      properties:
        id:
          type: string
        name:
          type: string
        slug:
          type: string
        status:
          type: string
        current_phase:
          type: string
        cycle:
          $ref: "#/components/schemas/Cycle"
          x-go-type-skip-optional-pointer: false
        active_run:
          type: integer
        run_count:
          type: integer
        repos:
          type: array
          items:
            type: string
        created_at:
          type: string
          format: date-time
        checkpoints:
          $ref: "#/components/schemas/Checkpoints"
        progress:
          $ref: "#/components/schemas/FeatureProgress"
        warnings:
          type: array
          items:
            $ref: "#/components/schemas/Warning"
    ModelDefaults:
      x-go-type: config.ModelConfig
      x-go-type-import:
        path: github.com/doordash-oss/agentic-orchestrator/internal/config
        name: config
      type: object
      properties:
        inquiry:
          type: string
        research:
          type: string
        planning:
          type: string
        implementation:
          type: string
        review:
          type: string
        utilities:
          type: string
        kb_build:
          type: string
        automatic_review:
          type: string
    Timing:
      type: object
      required: [total_seconds, by_phase]
      properties:
        total_seconds:
          type: integer
          format: int64
        by_phase:
          type: object
          additionalProperties:
            type: integer
            format: int64
    Cost:
      type: object
      required: [total_usd, by_phase]
      properties:
        total_usd:
          type: number
          format: double
          x-go-name: TotalUSD
        by_phase:
          type: object
          additionalProperties:
            type: number
            format: double
    VerificationItem:
      type: object
      required: [name, state]
      properties:
        name:
          type: string
        state:
          type: string
    ReviewGate:
      type: object
      required: [reviewing_gate, review_fixing, validating_plan]
      properties:
        reviewing_gate:
          type: boolean
        review_fixing:
          type: boolean
        validating_plan:
          type: boolean
        validator_statuses:
          type: object
          additionalProperties:
            type: string
    Failure:
      type: object
      properties:
        type:
          type: string
        message:
          type: string
    NeedUserInputGate:
      type: object
      required: [open]
      properties:
        feature_id:
          type: string
        open:
          type: boolean
        scope:
          type: string
        repo_name:
          type: string
        cycle_type:
          type: string
        input_notifications:
          type: string
        iteration:
          type: integer
        summary:
          type: string
        questions:
          type: array
          items:
            $ref: "#/components/schemas/NeedUserInputQuestion"
    NeedUserInputQuestion:
      type: object
      properties:
        index:
          type: integer
        prompt:
          type: string
        answer:
          type: string
    RepoStatus:
      type: object
      required: [name, touched, publishable]
      properties:
        name:
          type: string
        freshness:
          type: string
        rebase_status:
          type: string
        rebase_target:
          type: string
        conflict_files:
          type: array
          items:
            type: string
        touched:
          type: boolean
        pr_url:
          type: string
          x-go-name: PRURL
        last_error:
          type: string
        publishable:
          type: boolean
        cycle_type:
          type: string
        cycle_status:
          type: string
    RunSummary:
      type: object
      required: [run_number, artifact_count]
      properties:
        run_number:
          type: integer
        started_at:
          type: string
          format: date-time
          x-go-type-skip-optional-pointer: false
        sealed_at:
          type: string
          format: date-time
          x-go-type-skip-optional-pointer: false
        seal_reason:
          type: string
        current_phase:
          type: string
        phase_status:
          type: string
        iteration:
          type: integer
        roadmap_phase:
          type: integer
        roadmap_total:
          type: integer
        pending_review_phase:
          type: string
        pending_rewind_review_roadmap_phase:
          type: integer
        is_rewind:
          type: boolean
        artifact_count:
          type: integer
        has_need_user_gate:
          type: boolean
        setup:
          $ref: "#/components/schemas/Setup"
          x-go-type-skip-optional-pointer: false
    ActionScope:
      type: object
      required: [type]
      properties:
        type:
          type: string
        repo_selection:
          type: string
        cycle_type:
          type: string
    ActionInput:
      type: object
      required: [name, kind, required]
      properties:
        name:
          type: string
        kind:
          type: string
        required:
          type: boolean
        options:
          type: array
          items:
            type: string
        max_length:
          type: integer
    ActionDisabledReason:
      type: object
      required: [code, message]
      properties:
        code:
          type: string
        message:
          type: string
    Action:
      type: object
      required: [id, enabled, scope, required_inputs]
      properties:
        id:
          type: string
        enabled:
          type: boolean
        scope:
          $ref: "#/components/schemas/ActionScope"
        required_inputs:
          type: array
          items:
            $ref: "#/components/schemas/ActionInput"
        disabled_reasons:
          type: array
          items:
            $ref: "#/components/schemas/ActionDisabledReason"
    FeatureDetail:
      allOf:
        - $ref: "#/components/schemas/FeatureSummary"
        - type: object
          required:
            - models
            - automatic_review
            - historical_runs
            - repo_status
            - timing
            - cost
            - review_gate
            - actions
            - revision
            - cache_revalidate
          properties:
            description:
              type: string
            summary:
              type: string
            pipeline:
              type: string
            models:
              $ref: "#/components/schemas/ModelDefaults"
            automatic_review:
              $ref: "#/components/schemas/AutomaticReviewState"
            active_run_detail:
              $ref: "#/components/schemas/RunSummary"
              x-go-type-skip-optional-pointer: false
            historical_runs:
              type: array
              items:
                $ref: "#/components/schemas/RunSummary"
            repo_status:
              type: array
              items:
                $ref: "#/components/schemas/RepoStatus"
            cycle:
              $ref: "#/components/schemas/Cycle"
              x-go-type-skip-optional-pointer: false
            timing:
              $ref: "#/components/schemas/Timing"
            cost:
              $ref: "#/components/schemas/Cost"
            review_gate:
              $ref: "#/components/schemas/ReviewGate"
            verification_items:
              type: array
              items:
                $ref: "#/components/schemas/VerificationItem"
            failure:
              $ref: "#/components/schemas/Failure"
              x-go-type-skip-optional-pointer: false
            need_user_input:
              $ref: "#/components/schemas/NeedUserInputGate"
              x-go-type-skip-optional-pointer: false
            actions:
              type: array
              items:
                $ref: "#/components/schemas/Action"
            revision:
              type: string
            cache_revalidate:
              type: string
    Usage:
      type: object
      properties:
        input_tokens:
          type: integer
        output_tokens:
          type: integer
        cost_usd:
          type: number
          format: double
          x-go-name: CostUSD
    SessionSummary:
      type: object
      required:
        - id
        - feature_id
        - phase
        - kind
        - status
        - started_at
        - usage
      properties:
        id:
          type: string
        feature_id:
          type: string
        phase:
          type: string
        repo:
          type: string
        kind:
          type: string
        label:
          type: string
        provider:
          type: string
        model:
          type: string
        status:
          type: string
        turn_state:
          type: string
        started_at:
          type: string
          format: date-time
        iteration:
          type: integer
        context_percentage:
          type: integer
          x-go-name: ContextPct
        effort:
          type: string
        effort_source:
          type: string
        usage:
          $ref: "#/components/schemas/Usage"
    Cursor:
      type: object
      required: [total, start, end]
      properties:
        total:
          type: integer
        start:
          type: integer
        end:
          type: integer
    AskUserOption:
      type: object
      properties:
        label:
          type: string
        description:
          type: string
        confidence:
          type: number
          format: double
          x-go-type-skip-optional-pointer: false
    AskUserQuestion:
      type: object
      properties:
        question:
          type: string
        header:
          type: string
        multi_select:
          type: boolean
        options:
          type: array
          items:
            $ref: "#/components/schemas/AskUserOption"
    PermissionRememberPreview:
      type: object
      required: [pattern, scope, scope_display]
      properties:
        pattern:
          type: string
        scope:
          type: string
          description: Existing permission cache scope. Empty string means global.
        scope_display:
          type: string
    ControlRequest:
      type: object
      required: [request_id, tool_name, status]
      properties:
        request_id:
          type: string
        session_id:
          type: string
        feature_id:
          type: string
        phase:
          type: string
        tool_name:
          type: string
        status:
          type: string
        summary:
          type: string
        input:
          type: object
          additionalProperties: true
        questions:
          type: array
          items:
            $ref: "#/components/schemas/AskUserQuestion"
        remember:
          $ref: "#/components/schemas/PermissionRememberPreview"
          x-go-type-skip-optional-pointer: false
    SessionDetail:
      allOf:
        - $ref: "#/components/schemas/SessionSummary"
        - type: object
          required:
            - transcript_cursor
            - pending_controls
            - can_attach
            - log_available
          properties:
            transcript_cursor:
              $ref: "#/components/schemas/Cursor"
            pending_controls:
              type: array
              items:
                $ref: "#/components/schemas/ControlRequest"
            initial_prompt:
              type: string
            can_attach:
              type: boolean
            log_available:
              type: boolean
            safe_error:
              type: string
